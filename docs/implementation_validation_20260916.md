# 구현 및 운영 검증 (2026-09-16, KST)

`next_implementation_plan.md`의 1~4단계는 각각 다음 커밋으로 구현했다.

| 단계 | 커밋 | 핵심 변경 |
| --- | --- | --- |
| 기존 14일 추적 보존 | `86214d5` | 기존 작업 트리의 최근 5개 수집 및 14일 추적 변경을 검증해 먼저 커밋 |
| 1. 판정 기준 | `b13c87b` | SQLite 기준 버전, 서버 판정, 과거 최대 30개 기준선, 웹/API 동기화 |
| 2. 관측·성장 | `0d45a22` | 성공/실패 관측, 결측 구분, 상승 속도·가속도, D+N 추적 |
| 3. 소스 품질 | `d7e544e` | 관련성·재사용성 기준, 플랫폼 분산, 중복 제거, 후보별 판정 근거 |
| 4. 운영 안정성 | `52980b9` | SQLite 작업 큐·마이그레이션·WAL, 알림·계정 상태, 안전한 정리 dry-run |

전체 테스트 40개, `py_compile hotpost/*.py`, `node --check web/app.js`, `node --check web/accounts.js`, `git diff --check`가 통과했다. 임시 DB에서 큐 재시작과 성공 결과 다운로드, 중복 작업 차단, 동시 접근, 보존 대상 제외 정리를 검증했다. 실제 서버도 재시작한 뒤 기준·수집 상태·작업·알림·정리 dry-run API가 모두 HTTP 200을 반환했다.

기존 DB를 직접 마이그레이션한 결과 스키마 버전은 3이다. 실제 수집 전 12개 계정, 360개 게시물, 420개 스냅샷, 22개 활성 릴스 추적 대상이 있었다. 최신 5개 수집을 실행해 12개 중 6개 계정 성공, 6개 계정 실패, 39개 게시물을 수집했다. 이후 385개 게시물, 459개 스냅샷, 30개 활성 추적 대상이며, 기존 22개 대상과 이전 420개 스냅샷이 모두 남아 있다. 실패한 6개 계정은 Instagram `PolarisProfilePostsQuery` 서버 오류(`field_exception` 또는 `1357031`)를 반환했다. 리포트는 판정 v1, 분석 게시물 292개, 핫 게시물 50개다.

동일한 292개 게시물과 동일한 생성 시각에서 이전 판정과 현재 판정을 비교했다. 이전 방식은 핫 65개, 현재 방식은 핫 50개이며, 이전 방식에만 포함된 게시물은 20개, 현재 방식에만 포함된 게시물은 5개다. 등급이 바뀐 게시물은 38개, 배수 차이의 중앙값은 0.145다. 비교 방법과 주요 변경 건은 [판정 비교 데이터](scoring_comparison_20260916.json)에 기록했다. 이전 방식은 혼합 시점의 최대 60개 기준선을 사용했고, 현재 방식은 게시물 게시 이전의 최대 30개를 사용한다.

기존 소스 작업 한 건(`Dcp3P-WyMFe`)의 다운로드 영상 20개를 새 품질 기준으로 오프라인 재평가했다. 이전 분류는 클린 1개, 약한 오버레이 4개, 편집된 영상 15개였다. 새 기준을 통과한 영상은 클린 1개와 약한 오버레이 2개, 총 3개였고 편집된 영상은 0개다. 이는 한 릴스의 기존 후보 재평가이며, 대표 릴스 3개에 대한 새 검색 결과는 아니다.

대표 릴스 `DdOPIJgKPIN`, `Dcp3P-WyMFe`, `Dc4kwGNT-w_`의 실제 소스 탐색을 각각 시도했지만 기준 영상의 Instagram `media/info` 요청이 모두 HTTP 400으로 거부됐다. 같은 세션의 Instaloader 경로도 세 건 모두 `checkpoint_required`를 반환했다. 로그인 또는 체크포인트를 우회하지 않았고, 세션이 정상화되면 세 건을 다시 실행해야 소스 품질 완료 조건을 확인할 수 있다.

LaunchAgent는 07:00에 설치·로드된 상태다. 이 기록을 만든 시각은 02:45 KST 전후로, 다음 07:00 실행은 아직 발생하지 않았다. `data/daily_collect.log`도 아직 없어 다음 예약 실행의 결과는 그 시각 이후 확인해야 한다. 실제 데이터 정리는 실행하지 않았고, dry-run은 0건·예상 회수량 0바이트였다.
